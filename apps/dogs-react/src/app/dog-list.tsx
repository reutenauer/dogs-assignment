import React from 'react';
import styled from 'styled-components';

const StyledDogList = styled.div`
  
`;

export function DogList() {
  return <StyledDogList>DogList works!</StyledDogList>;
}

export default DogList;
